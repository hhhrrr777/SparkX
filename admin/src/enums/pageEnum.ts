export enum PageEnum {
  // 登录
  BASE_LOGIN = '/login',
  BASE_LOGIN_NAME = 'Login',
  //重定向
  REDIRECT = '/redirect',
  REDIRECT_NAME = 'Redirect',
  // 首页（登录后默认进入：新对话）
  BASE_HOME = '/chat',
  //首页跳转默认路由
  BASE_HOME_REDIRECT = '/chat/index',
  // 错误
  ERROR_PAGE_NAME = 'ErrorPage',
}
